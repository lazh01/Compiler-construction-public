# Typecheck of use of null, that the type of null is not compatible with
# the integer type
# NullWrong.out changed to only write "error", as that is the intended output.
var a: array of int;
var b: int;

allocate a of length 10;
b = null;
write 42;
