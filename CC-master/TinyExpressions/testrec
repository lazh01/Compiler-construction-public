# Typecheck of records, simple example of use of records
type recordType = record of {
                    x: int,
                    y: int
                  };
var p : recordType;

allocate p;

p.x = 1;
write p.x;
